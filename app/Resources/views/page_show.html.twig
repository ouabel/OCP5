{% extends 'layout.html.twig' %}
{% block title %}{{ page.title }}{% endblock %}

{% block container %}
	<section>
		<header>
			<h1>{{ page.title }}</h1>
		</hedaer>
		<div>
			{{ page.content|raw }}
		</div>
	<section>

{% endblock %}